app:
  env: testing
  debug: false

rdf:
  dataset: semantic_forge_test
  timeout:
    default: 10
    max: 30

postgres:
  schema: rdf_acl_test
  migrations_table: rdf_acl_migrations

redis:
  namespace: semanticforge_test




